version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cleartoo-app
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file:
      - .env
    volumes:
      - .:/var/www
      - app-storage:/var/www/storage
      - app-bootstrap:/var/www/bootstrap/cache
      - app-uploads:/var/www/public/uploads
    depends_on:
      db:
        condition: service_healthy
    networks:
      - cleartoo-network

  db:
    image: mysql:5.7
    container_name: cleartoo-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: cleartoo
      MYSQL_ROOT_PASSWORD: Aa32c2PFMzcwDd2r
      MYSQL_USER: cleartoo
      MYSQL_PASSWORD: Aa32c2PFMzcwDd2r
    volumes:
      - db-data:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-uroot",
          "-pAa32c2PFMzcwDd2r",
          "--silent",
        ]
      interval: 5s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - cleartoo-network

networks:
  cleartoo-network:
    driver: bridge

volumes:
  db-data:
  app-storage:
  app-bootstrap:
  app-uploads:
